# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'
use_frameworks!

target 'Autocue' do
  pod 'IQKeyboardManager'
  pod 'SnapKit'
  pod "FMDB"
  pod 'ObjectMapper'
end
